box: debian
build:
  steps:
    - install-packages:
        packages: git
    - script:
        name: download theme
        code: |
            $(git clone https://github.com/hdcdstr8fwd/foundation-theme ./themes/foundation-theme)
    - script:
        name: build site
        code: |
            $(hugo)
